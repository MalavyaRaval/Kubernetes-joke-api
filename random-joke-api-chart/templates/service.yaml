apiVersion: v1
kind: Service
metadata:
  name: {{ include "random-joke-api-chart.fullname" . }}
  labels:
    {{- include "random-joke-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "random-joke-api-chart.selectorLabels" . | nindent 4 }}
