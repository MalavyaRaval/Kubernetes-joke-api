apiVersion: v1
kind: Service
metadata:
  name: random-joke-api-service
spec:
  selector:
    app: random-joke-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
